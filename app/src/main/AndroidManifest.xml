<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cpsu.sports">

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.AthleteMedalList"
            android:label="Medal List"/>
        <activity android:name=".activities.SportList" />
        <activity android:name=".activities.GameDetail" />
        <activity android:name=".activities.MedalTallyActivity" />
        <activity android:name=".activities.MedalDetail" />
        <activity android:name=".activities.AthleteDetail" />
        <activity android:name=".activities.AthleteList" />
        <activity android:name=".activities.CollegeList" />
        <activity android:name=".activities.CollegeDetail" />
        <activity android:name=".activities.SportDetail" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>